# WhatsApp scraper config

# Location where Chrome will store your WhatsApp login session
USER_DATA_DIR=./User_Data

# Where JSON files will be saved
OUT_DIR=./logs

# Comma-separated list of group names you want to capture
GROUP_NAMES=Testing automation, Team Updates, Office Group

# How many times to scroll up each visit to load history
BACKFILL_SCROLLS=2

# Run in headless Chrome? (true/false)
HEADLESS=false

# How often (seconds) to loop over all groups
POLL_SECS=5

# Rotate log files daily? (true/false)
ROTATE_DAILY=false
